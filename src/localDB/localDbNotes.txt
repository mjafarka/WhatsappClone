
for adding object to the persons
---------------------------------

* enqueue
    to add object to the array based on a sorting function

* moveto front
    when we click one object. it should be removed from the current index and moved to the 
    front.